# 📌 คู่มือ: วิธีเพิ่มการแจ้งเตือนใหม่

## สำหรับมือใหม่ — อธิบายให้เข้าใจง่าย

---

## 🔍 ประเภทของการแจ้งเตือนปัจจุบัน

### 1. **"หัวหน้างานถูกเปลี่ยน"** (leader_change)
- **ส่งให้**: ช่าง (user / technician)
- **เหตุการณ์เกิด**: เมื่อ Admin เปลี่ยนหัวหน้างาน
- **ไฟล์ที่เก็บโค้ด**: `src/contexts/JobContext.tsx`
- **บรรทัดประมาณ**: 160-177 ในฟังก์ชัน `updateJob()`
- **ตัวอย่าง**:
  ```
  "หัวหน้างานถูกเปลี่ยน"
  "งาน [ชื่องาน] เปลี่ยนหัวหน้างานเป็น [ชื่อหัวหน้างานใหม่] โดย [ชื่อ Admin]"
  ```

### 2. **"มีการเปลี่ยนหัวหน้างาน"** (leader_reassignment)
- **ส่งให้**: หัวหน้างานเดิม (leader ที่ถูกเปลี่ยน)
- **เหตุการณ์เกิด**: เมื่อ Admin เปลี่ยนหัวหน้างาน
- **ไฟล์ที่เก็บโค้ด**: `src/contexts/JobContext.tsx`
- **บรรทัดประมาณ**: 179-194
- **ตัวอย่าง**:
  ```
  "มีการเปลี่ยนหัวหน้างาน"
  "งาน [ชื่องาน] ถูกเปลี่ยนให้ [ชื่อหัวหน้างานใหม่] ดูแลแทนคุณ"
  ```

### 3. **"คุณได้รับมอบหมายเป็นหัวหน้างานใหม่"** (leader_assignment)
- **ส่งให้**: หัวหน้างานใหม่ (leader ที่ได้รับมอบหมาย)
- **เหตุการณ์เกิด**: เมื่อ Admin เปลี่ยนหัวหน้างาน
- **ไฟล์ที่เก็บโค้ด**: `src/contexts/JobContext.tsx`
- **บรรทัดประมาณ**: 196-209
- **ตัวอย่าง**:
  ```
  "คุณได้รับมอบหมายเป็นหัวหน้างานใหม่"
  "คุณได้รับมอบหมายให้ดูแลงาน [ชื่องาน] จาก [ชื่อหัวหน้างานเดิม]"
  ```

### 4. **"ได้รับมอบหมายงานใหม่"** (team_assignment_added)
- **ส่งให้**: ช่าง (user) ที่ถูกเพิ่มเข้าทีม
- **เหตุการณ์เกิด**: เมื่อ Leader เพิ่มช่างเข้าทีมงาน
- **ไฟล์ที่เก็บโค้ด**: `src/components/leader/LeaderJobDetailDialog.tsx`
- **บรรทัดประมาณ**: 126-138 ในฟังก์ชัน `handleConfirmTeamChange()`
- **ตัวอย่าง**:
  ```
  "ได้รับมอบหมายงานใหม่"
  "คุณถูกเพิ่มเข้าทีมงาน '[ชื่องาน]' โดย [ชื่อ Leader]"
  ```

### 5. **"มีการถอดคุณออกจากงาน"** (team_assignment_removed)
- **ส่งให้**: ช่าง (user) ที่ถูกถอดออกจากทีม
- **เหตุการณ์เกิด**: เมื่อ Leader ถอดช่างออกจากทีมงาน
- **ไฟล์ที่เก็บโค้ด**: `src/components/leader/LeaderJobDetailDialog.tsx`
- **บรรทัดประมาณ**: 140-152
- **ตัวอย่าง**:
  ```
  "มีการถอดคุณออกจากงาน"
  "คุณถูกถอดออกจากทีมงาน '[ชื่องาน]' โดย [ชื่อ Leader]"
  ```

---

## 🛠️ ขั้นตอนการเพิ่มการแจ้งเตือนแบบใหม่

### **ขั้นตอนที่ 1: หาตำแหน่งที่ต้องการส่งการแจ้งเตือน**
ตัวอย่างสถานการณ์:
- "เมื่อสถานะงานเปลี่ยนเป็น 'เสร็จสิ้น'"
- "เมื่อลูกค้าโทรมาและต้องเปลี่ยนวันส่งงาน"
- "เมื่อมีการเพิ่มรูปภาพไปยังงาน"

ไฟล์ที่ต้องแก้ไขคือที่เกิดเหตุการณ์นั้น ๆ

**ตัวอย่าง**: ถ้าต้องการแจ้งเตือนเมื่อเปลี่ยนสถานะ → แก้ใน `src/contexts/JobContext.tsx` ในฟังก์ชัน `updateJob()`

---

### **ขั้นตอนที่ 2: เรียก `addNotification()` ด้วย Object ที่ถูกต้อง**

#### โครงสร้างของ Notification Object:

```typescript
{
  title: "ชื่อเรื่องสั้น ๆ",
  message: "ข้อความรายละเอียด (มักจะอธิบายว่าเกิดอะไรขึ้น)",
  recipientRole: "user" | "leader" | "admin" | "executive",  // ใครเป็นผู้รับ
  recipientId?: "id_ของ_user" | "id_ของ_leader",            // (ถ้ามี) ส่งให้คนรับเฉพาะตัว
  relatedJobId?: "JOB-20250703-A4T9",                        // (ถ้ามี) id ของงานที่เกี่ยวข้อง
  metadata?: {
    type: "your_event_type",  // ชื่อประเภทเหตุการณ์
    // ... ข้อมูลเพิ่มเติมอื่น ๆ
  }
}
```

#### ตัวอย่าง: เพิ่มการแจ้งเตือนเมื่อสถานะงานเปลี่ยนเป็น "เสร็จสิ้น"

```tsx
// ในฟังก์ชัน updateJob() ใน src/contexts/JobContext.tsx

if (updatedData.status === 'done' && targetJob.status !== 'done') {
  // แจ้งเตือนให้ Leader ที่ดูแลงานนี้
  addNotification({
    title: "งานเสร็จสิ้น",
    message: `งาน "${targetJob.title}" ได้ส่งเสร็จสิ้นแล้ว`,
    recipientRole: "leader",
    recipientId: String(targetJob.leadId),
    relatedJobId: targetJob.id,
    metadata: {
      type: "job_completed",
      jobId: targetJob.id,
      completedAt: new Date().toISOString(),
    }
  });
}
```

---

### **ขั้นตอนที่ 3: เข้าใจ Properties ของ `recipientRole`**

| recipientRole | ความหมาย | ตัวอย่าง |
|---|---|---|
| `"admin"` | ผู้ดูแลระบบ | ส่งแจ้งเตือนให้ Admin ทุกคน |
| `"leader"` | หัวหน้าทีม / ผู้จัดการ | ส่งให้หัวหน้าที่ดูแลงาน |
| `"user"` | ช่าง / เทคนิเซียน | ส่งให้ช่างที่ได้รับมอบหมายงาน |
| `"executive"` | ผู้บริหาร | ส่งให้ผู้บริหารระดับสูง |

---

## 💡 ตัวอย่างข้อ Case การแจ้งเตือนที่อาจเพิ่มเติม

### **Case 1: เปลี่ยนวันส่งงาน**
```tsx
addNotification({
  title: "วันส่งงานถูกเปลี่ยน",
  message: `วันส่งงาน "${job.title}" เปลี่ยนเป็น ${format(updatedData.endDate, 'dd/MM/yyyy')}`,
  recipientRole: "leader",
  recipientId: String(job.leadId),
  relatedJobId: job.id,
  metadata: { type: "date_changed", oldDate: job.endDate, newDate: updatedData.endDate }
});
```

### **Case 2: มีการเพิ่มรูปภาพใหม่**
```tsx
addNotification({
  title: "มีรูปภาพใหม่สำหรับงาน",
  message: `รูปภาพใหม่ถูกเพิ่มเข้าไปในงาน "${job.title}"`,
  recipientRole: "user",
  recipientId: techId,  // ช่างแต่ละคน
  relatedJobId: job.id,
  metadata: { type: "image_added" }
});
```

### **Case 3: สถานะงานเปลี่ยนเป็น "กำลังดำเนินการ"**
```tsx
addNotification({
  title: "งานเริ่มดำเนินการแล้ว",
  message: `หัวหน้างาน ${job.leadId} ได้รับทราบงาน "${job.title}" และเริ่มดำเนินการแล้ว`,
  recipientRole: "admin",
  relatedJobId: job.id,
  metadata: { type: "job_acknowledged" }
});
```

---

## 📍 ตำแหน่งไฟล์ที่สำคัญ

| ไฟล์ | ที่อยู่ | ความสำคัญ |
|---|---|---|
| **NotificationContext.tsx** | `src/contexts/NotificationContext.tsx` | 🔥 ศูนย์กลางจัดการการแจ้งเตือน |
| **JobContext.tsx** | `src/contexts/JobContext.tsx` | 🔥 ส่วนใหญ่ของการแจ้งเตือนเกี่ยวกับงาน |
| **LeaderJobDetailDialog.tsx** | `src/components/leader/LeaderJobDetailDialog.tsx` | 🔥 การแจ้งเตือนเกี่ยวกับการเปลี่ยนทีม |
| **Notification.tsx** (Display) | `src/pages/commom/Notification.tsx` | หน้าแสดงการแจ้งเตือน (แก้ไขเพื่อเปลี่ยนวิธีแสดงผล) |
| **NotificationItem Type** | `src/types/index.ts` | ประเภทข้อมูลของการแจ้งเตือน |

---

## 🎯 ขั้นตอนสรุป

1. **หา**: ตัวอย่างการแจ้งเตือนที่มีอยู่ในไฟล์ที่สำคัญ
2. **เข้าใจ**: โครงสร้างของ `addNotification()` object
3. **ทำ**: คัดลอกตัวอย่างที่เหมาะสมและแก้ไข property ให้ตรงกับ use case ของคุณ
4. **ทดสอบ**: เพิ่มการแจ้งเตือนใหม่ที่ต้องการ

---

## ❓ ถ้าค้าง

- ลองดูตัวอย่างในไฟล์ `src/contexts/JobContext.tsx` (line 160-200)
- ลองดูตัวอย่างในไฟล์ `src/components/leader/LeaderJobDetailDialog.tsx` (line 120-150)
- ถ้ายังงง ลองค้นหาคำว่า `addNotification(` ในโปรเจกต์เพื่อเห็นตัวอย่างอื่น ๆ
